# See: https://docs.github.com/en/actions/writing-workflows
---
name: Update Scoop bucket

on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string
      assets:
        required: true
        type: string

jobs:
  update-scoop-bucket:
    uses: asimov-platform/actions/workflows/update-scoop-bucket.yaml@master
    secrets: inherit
    with:
      version: ${{ inputs.version }}
      assets: ${{ inputs.assets }}
